import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Sidebar from "../components/Sidebar";
import { Toaster } from "sonner";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "TrustFix AI | Community Infrastructure Intelligence",
  description: "Diagnose, report, and repair civic and home infrastructure using Multimodal AI.",
  openGraph: {
    title: "TrustFix AI",
    description: "Community Decision Intelligence powered by AI",
    type: "website",
  }
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full">
        <Sidebar>{children}</Sidebar>
        <Toaster position="top-center" richColors theme="light" />
      </body>
    </html>
  );
}
